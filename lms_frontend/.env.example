# OceanLMS Frontend - Example Environment Variables

# Frontend/Backend URLs
REACT_APP_FRONTEND_URL=http://localhost:3000
REACT_APP_BACKEND_URL=http://localhost:8080

# Optional: Feature flags as JSON or comma list
# Example JSON: {"FLAG_SUPABASE_MODE":true}
REACT_APP_FEATURE_FLAGS=

# Supabase browser mode (only when FLAG_SUPABASE_MODE=true)
# REACT_APP_SUPABASE_URL should be your Supabase project URL
REACT_APP_SUPABASE_URL=
# REACT_APP_SUPABASE_KEY must be the PUBLIC ANON KEY only (never service role)
REACT_APP_SUPABASE_KEY=

# Other container vars that may be used
REACT_APP_NODE_ENV=development
REACT_APP_ENABLE_SOURCE_MAPS=true
REACT_APP_HEALTHCHECK_PATH=/health
