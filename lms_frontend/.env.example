# Public frontend environment variables (no secrets)
# These are consumed at build-time by CRA (React Scripts).
# Do not set Supabase anon keys in the frontend. All calls go through a secure backend proxy using cookie-based sessions.

# Base URL for backend proxy (e.g., https://api.oceanlms.example.com)
REACT_APP_BACKEND_URL=http://localhost:8080

# Deployed frontend app URL (used for login redirect target)
REACT_APP_FRONTEND_URL=http://localhost:3000

# Optional logging level: debug|info|warn|error
REACT_APP_LOG_LEVEL=info
