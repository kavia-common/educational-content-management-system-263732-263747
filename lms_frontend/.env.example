# Frontend environment example for OceanLMS

# Frontend/Backend URLs (used in proxy mode and redirects)
REACT_APP_FRONTEND_URL=http://localhost:3000
REACT_APP_BACKEND_URL=http://localhost:8080

# Feature flags can be JSON or comma-separated list
# Enable direct Supabase mode by setting FLAG_SUPABASE_MODE=true
# Example JSON:
# REACT_APP_FEATURE_FLAGS={"FLAG_SUPABASE_MODE":true}
# Example list:
# REACT_APP_FEATURE_FLAGS=charts,newDashboard,FLAG_SUPABASE_MODE
REACT_APP_FEATURE_FLAGS=
REACT_APP_EXPERIMENTS_ENABLED=false

# Optional health path
REACT_APP_HEALTHCHECK_PATH=/health

# Supabase browser client configuration (used only when FLAG_SUPABASE_MODE is enabled)
# These are public "anon" settings; NO service role key should be present in frontend.
REACT_APP_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
REACT_APP_SUPABASE_ANON_KEY=YOUR_PUBLIC_ANON_KEY
