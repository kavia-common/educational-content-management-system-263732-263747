# Frontend environment example (do not commit real secrets)

# Backend proxy base URL (if used)
REACT_APP_BACKEND_URL=http://localhost:8080
REACT_APP_FRONTEND_URL=http://localhost:3000

# Feature flags (JSON or comma list)
# Example to enable browser Supabase mode:
# REACT_APP_FEATURE_FLAGS={"FLAG_SUPABASE_MODE":true}
REACT_APP_FEATURE_FLAGS=

# Supabase (only used when FLAG_SUPABASE_MODE=true)
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_KEY=

# Optional
REACT_APP_HEALTHCHECK_PATH=/health
